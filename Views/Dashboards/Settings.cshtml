@using AspnetCoreMvcFull.Models;
@model PartialModel;

@{
    ViewData["Title"] = "Configurações";
}

@section VendorStyles {
    <link rel="stylesheet" href="~/vendor/libs/apex-charts/apex-charts.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/vendor/css/configuracoes.css">
}

<div class="container mt-5">
    <div class="settings-header">
        <h2>@ViewData["Title"]</h2>
        <p>Gerencie suas preferências e configurações da conta</p>
    </div>

    <div class="card shadow-sm mt-4">
        <div class="card-body">
            <div class="settings-section-title">Conta</div>
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="settings-info-label"><i class="icon fas fa-user"></i>Nome:</label>
                        <p class="settings-info-value">@Model.funcionario.Nome</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="settings-info-label"><i class="icon fas fa-envelope"></i>Email:</label>
                        <p class="settings-info-value">@Model.funcionario.Email</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm mt-4">
        <div class="card-body">
            <div class="settings-section-title">Segurança</div>
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label for="currentPassword" class="settings-info-label"><i class="icon fas fa-lock"></i>Senha Atual:</label>
                        <input type="password" class="form-control" id="currentPassword" placeholder="Digite sua senha atual">
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label for="newPassword" class="settings-info-label"><i class="icon fas fa-key"></i>Nova Senha:</label>
                        <input type="password" class="form-control" id="newPassword" placeholder="Digite sua nova senha">
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label for="confirmPassword" class="settings-info-label"><i class="icon fas fa-check"></i>Confirmar Senha:</label>
                        <input type="password" class="form-control" id="confirmPassword" placeholder="Confirme sua nova senha">
                    </div>
                </div>
                <div class="col-md-12">
                    <button class="btn btn-primary mt-3">Atualizar Senha</button>
                </div>
            </div>
        </div>
    </div>

@section VendorScripts {
    <script src="~/vendor/libs/apex-charts/apexcharts.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
}

@section PageScripts {
    <script src="~/js/settings.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // JS code to handle password update and preferences save
        document.querySelector('button.btn-primary').addEventListener('click', function () {
            // Code to handle update
        });
    </script>
}
